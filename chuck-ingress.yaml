apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: chuck-facts-ingress
spec:
  rules:
    - host: localhost
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: chuck-facts-service
                port: 
                  number: 48080
